name: Keep Server Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # ping every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sf https://your-app.onrender.com || echo "Ping failed (non-fatal)"
